// ==============================================================================
// KEYBINDING PHILOSOPHY
// ==============================================================================
// Mnemonic approach: Ctrl-<first-letter> for mode switches
// - Ctrl-m: Move mode (reposition panes)
// - Ctrl-R: Resize mode
// - Ctrl-p: Pane mode
//
// Vim-style navigation: hjkl throughout for directional movement
// Ctrl-based global actions: Avoids Alt conflicts with other programs
// Tab navigation: Ctrl-[ and Ctrl-] for prev/next tab focus
// ==============================================================================

keybinds {

    // mnemonic of 'm' for move
    move {
      bind "n" "Tab" { MovePane; }
      bind "p" "Shift Tab" { MovePaneBackwards; }
      bind "h" "Left" { MovePane "Left"; }
      bind "j" "Down" { MovePane "Down"; }
      bind "k" "Up" { MovePane "Up"; }
      bind "l" "Right" { MovePane "Right"; }
    }
    shared_except "locked" {
      unbind "Ctrl h"   //original switch from move to normal
      bind "Ctrl m" { SwitchToMode "Normal"; }
    }

    pane {
      // prefer arrows for new panes, and r for rename
      unbind "e" "c" "d" "w" "Up" "Left"
      bind "h" { MoveFocus "left"; }
      bind "j" { MoveFocus "down"; }
      bind "k" { MoveFocus "up"; }
      bind "l" { MoveFocus "right"; }
      bind "-" { NewPane "Down"; SwitchToMode "normal"; }
      bind "_" { NewPane "Down"; SwitchToMode "normal"; }
      bind "\\" { NewPane "Right"; SwitchToMode "normal"; }
      bind "|" { NewPane "Right"; SwitchToMode "normal"; }
      bind "s" { NewPane "stacked"; SwitchToMode "normal"; }
      bind "r" { SwitchToMode "RenamePane"; PaneNameInput 0;}
    }

    // Resize
    resize {
        unbind "Ctrl n"  //original
        bind "Ctrl R" { SwitchToMode "Normal"; }  // lowercase conflicts with nvim
    }
    shared_except "resize" "locked" {
        unbind "Ctrl n"
        bind "Ctrl R" { SwitchToMode "Resize"; }
    }

    // Tab
    tab {
        bind "[" { GoToPreviousTab; }
        bind "]" { GoToNextTab; }
        bind "{" { MoveTab "Left"; }
        bind "}" { MoveTab "Right"; }
        // new and ask name
        bind "n" {
          LaunchOrFocusPlugin "file:~/.local/bin/zellij-newtab-plus.wasm" {
            floating true
            use_zoxide false
          };
          SwitchToMode "Normal"
        }
    
        bind "t" {
          LaunchOrFocusPlugin "https://github.com/imsnif/theme-tester/releases/latest/download/theme-tester.wasm"
          SwitchToMode "Normal"
        }


        // New, default name
        bind "N" { NewTab; SwitchToMode "Normal"; }
        bind "x" { CloseTab; SwitchToMode "Normal"; }
        bind "s" { ToggleActiveSyncTab; SwitchToMode "Normal"; }
    }

    // Shared
    shared_except "locked" {
        // REMOVE Alt keys conflicting with other programs
        unbind "Alt i" "Alt o"        // tab movement
        unbind "Alt p" "Alt Shift p"  // pane grouping

        // Resize (replacing Alt version)
        unbind "Alt =" "Alt +" "Alt -"
        bind "Ctrl -" { Resize "Decrease"; }
        bind "Ctrl =" { Resize "Increase"; }

        // Toggle pane frames (borders)
        bind "Ctrl u" { TogglePaneFrames; }

        // Fullscreen toggle (replacing Alt version)
        unbind "Alt f"
        bind "Ctrl f" { ToggleFocusFullscreen; }

        // Floating Pane toggle
        bind "Ctrl w" { ToggleFloatingPanes; }

        // Pane Focus Change (replacing Alt version)
        unbind "Alt h" "Alt Left" "Alt l" "Alt Right" "Alt j" "Alt Down" "Alt k" "Alt Up"
        bind "Ctrl h" { MoveFocus "left"; }
        bind "Ctrl j" { MoveFocus "down"; }
        bind "Ctrl k" { MoveFocus "up"; }
        bind "Ctrl l" { MoveFocus "right"; }

        //  New Pane (replacing Alt version)
        unbind "Alt n"
        bind "Ctrl _" { NewPane "Down"; }
        bind "Ctrl -" { NewPane "Down"; }
        bind "Ctrl \\" { NewPane "Right"; }
        bind "Ctrl |" { NewPane "Right"; }
        bind "Ctrl s" { NewPane "stacked"; }

        // New tab with name input
        bind "Ctrl n" {
          LaunchOrFocusPlugin "file:~/.local/bin/zellij-newtab-plus.wasm" {
            floating true
            use_zoxide false
          };
          SwitchToMode "Normal"
        }

        // Foget name 
        bind "Ctrl y" {
            LaunchOrFocusPlugin "file:~/.local/bin/zellij_forgot.wasm" {
                floating true
            }
        }

        // duplicated in tab menu without ctrl
        bind "Ctrl [" { GoToPreviousTab; }
        bind "Ctrl ]" { GoToNextTab; }
    }
}

plugins clear-defaults=true {
    compact-bar location="zellij:compact-bar" {
      tooltip "Ctrl x"
    }
}



//== LOOK AND FEEL

// theme_dir "/Users/jefwinds/.config/zellij/themes"
theme "tinted-theming"
default_layout "compact-top-bar"
session_serialization false
show_startup_tips false
show_release_notes false
// pane_frames false  // bug with this clears screen and moves cursor to bottom, so leave it default of true
ui {
    pane_frames {
        hide_session_name true
        rounded_corners true
    }
}

