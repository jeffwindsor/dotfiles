#!/usr/bin/env bash

# default source and target if missing
[ -z "$source" ] && source=$(dirname "$0") 
[ -z "$target" ] && target=$HOME

folder="${1}"
if [ ! -z "$folder" ]
then
    echo -e "\033[0;34m==> stowing $folder\033[0m"
    stow -d $source -t $target $folder
fi
