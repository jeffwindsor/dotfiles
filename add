#!/usr/bin/env sh

# default source and target if missing
[ -z "$source" ] && source=$(dirname "$0") 
[ -z "$target" ] && target=$HOME

# stow each folder passed in as an argument
for folder in "$@"
do
    echo "\033[0;34m==> stowing $folder\033[0m"
    stow -n -d $source -t $target $folder
done
